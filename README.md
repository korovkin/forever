# parallel

where `xargs` meets `foreman` or `goreman` for controlled, concurrent execution of shell commands.

## builds

## build:
```
go build cmd/*.go
```

or just:

```
make travis
```

## Demos:

```
TBD
```
